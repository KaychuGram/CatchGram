const create = require('./create');
const list = require('./list');
const update = require('./update');
const listUserPost = require('./listUserPost')
const destroy = require('./destroy');

module.exports = {
    create,
    list,
    update,
    listUserPost,
    destroy,
  };